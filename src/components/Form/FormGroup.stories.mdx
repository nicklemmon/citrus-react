import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { dollarSign } from 'react-icons-kit/feather/dollarSign'
import FormGroup from './FormGroup'
import FormGroupCap from './FormGroupCap'
import FormLabel from './FormLabel'
import FormControl from './FormControl'
import Icon from '../Icon'

<Meta title="Forms|FormGroup" component={FormGroup} />

# FormGroup

## Used for grouping smaller form components in to a functional whole

Use `<FormGroup/>` to wrap smaller form components for things like error handling and descriptions.

```jsx
import { FormGroup, FormLabel, FormControl } from '@citrus-react'

const MyComponent = () => (
  <FormGroup id="first-name" variant="text" description="This is a description">
    <FormLabel>First Name</FormLabel>

    <FormControl />
  </FormGroup>
)
```

<Preview>
  <Story name="with lotsa stuff">
    <FormGroup id="first-name" variant="text" description="This is a description">
      <FormGroupCap>
        <Icon icon={dollarSign} />
      </FormGroupCap>
      <FormLabel>First Name</FormLabel>
      <FormControl />
    </FormGroup>
  </Story>
</Preview>

## Variants

### Text

### Select

<Preview>
  <Story name="select variant">
    <FormGroup id="select-variant" variant="select">
      <FormLabel>Favorite Foods</FormLabel>
      <FormControl>
        <option>Pizza</option>
        <option>Tacos</option>
        <option>Avocado</option>
      </FormControl>
    </FormGroup>
  </Story>
</Preview>
